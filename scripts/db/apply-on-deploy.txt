# 배포 때 jar 를 교체하기 전에 순서대로 실행할 DDL 목록 (scripts/deploy-remote.sh 가 읽는다)
#
# 운영은 ddl-auto=validate 라 애플리케이션이 스키마를 만들지 않는다. 새 테이블이 필요한 버전을 배포하면
# 스키마 검증에서 기동이 실패하므로, 배포 파이프라인이 먼저 이 목록의 DDL 을 적용한다.
#
# 규칙
#   - 매 배포마다 다시 실행된다. 여러 번 실행해도 안전하게(IF NOT EXISTS 등) 작성할 것.
#   - 적용에 실패하면 jar 를 교체하지 않고 배포를 중단한다. 기존 버전은 그대로 떠 있다.
#   - 인덱스 추가처럼 운영 부하를 보고 시점을 고르고 싶은 DDL 은 여기 넣지 말고 수동으로 반영한다.
#   - # 으로 시작하는 줄과 빈 줄은 무시한다.
create-idempotency-records.sql
